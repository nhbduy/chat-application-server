const SERVER_NAME = '';

const SERVER_PORT = process.env.PORT || 5000;

const DATABASE_URL = '';

const DATABASE_NAME = '';

module.exports = {
  SERVER_NAME,
  SERVER_PORT,
  DATABASE_URL,
  DATABASE_NAME
};
